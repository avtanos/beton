# 🔧 Исправление CORS

## Проблема

При попытке создать компанию (или другую операцию POST/PATCH/DELETE) браузер отправляет preflight запрос (OPTIONS), который может блокироваться из-за CORS.

## ✅ Решение

### Улучшенная CORS конфигурация

Обновлена конфигурация CORS в `backend/main.py`:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:3000",
        "http://localhost:5173",
        "http://127.0.0.1:3000",
        "http://127.0.0.1:5173"
    ],
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
    allow_headers=["*"],
    expose_headers=["*"],
    max_age=3600,  # Кэш preflight запросов
)
```

### Что изменилось:

1. ✅ Добавлены адреса с `127.0.0.1` (помимо `localhost`)
2. ✅ Явно указаны все HTTP методы
3. ✅ Добавлен `expose_headers` для всех заголовков
4. ✅ Добавлен `max_age` для кэширования preflight запросов

## 🚀 Применение исправлений

### Шаг 1: Остановите backend

Нажмите `Ctrl+C` в терминале где запущен backend.

### Шаг 2: Перезапустите backend

```bash
cd backend
python main.py
```

### Шаг 3: Проверьте работу

1. Откройте http://localhost:3000
2. Попробуйте создать компанию/пользователя/проект
3. Проверьте консоль браузера (F12) - не должно быть CORS ошибок

## 🔍 Как проверить что CORS работает

### В консоли браузера (F12):

**Если CORS работает правильно:**
```
✅ Нет красных ошибок с "CORS policy"
✅ Запросы выполняются успешно
✅ Статус 200 или 201 для запросов
```

**Если есть проблема с CORS:**
```
❌ Access to fetch at 'http://localhost:8000/api/...' from origin 'http://localhost:3000' 
   has been blocked by CORS policy
```

### В консоли backend:

После перезапуска должны успешно проходить:
```
INFO:     127.0.0.1:xxxxx - "OPTIONS /api/companies HTTP/1.1" 200 OK
INFO:     127.0.0.1:xxxxx - "POST /api/companies HTTP/1.1" 200 OK
```

## 📝 Что такое preflight запрос?

**Preflight** - это предварительный OPTIONS запрос, который браузер отправляет перед "сложными" запросами (POST, PUT, PATCH, DELETE с custom headers).

**Зачем нужен:**
- Проверить разрешён ли метод
- Проверить разрешены ли заголовки
- Проверить разрешён ли origin

**Если preflight успешен (200 OK)**, браузер отправляет реальный запрос.

## 🔧 Дополнительные настройки

### Если всё ещё не работает:

#### 1. Проверьте что backend запущен на правильном порту

```bash
# Должно показать: Uvicorn running on http://0.0.0.0:8000
```

#### 2. Проверьте что frontend на правильном порту

```bash
# Vite обычно: http://localhost:3000
# Или: http://localhost:5173
```

#### 3. Очистите кэш браузера

```
Ctrl + Shift + Delete → Очистить кэш
Или перезапустите браузер
```

#### 4. Попробуйте другой браузер

Иногда проблема в кэше конкретного браузера.

## 🌐 Разрешить все origins (только для разработки!)

**Внимание:** Не используйте в продакшене!

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # Разрешить все
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## ✅ Проверка работы API

### Тест через curl:

```bash
# OPTIONS запрос (preflight)
curl -X OPTIONS http://localhost:8000/api/companies \
  -H "Origin: http://localhost:3000" \
  -H "Access-Control-Request-Method: POST" \
  -v

# Должен вернуть 200 OK с заголовками CORS
```

```bash
# POST запрос
curl -X POST http://localhost:8000/api/companies \
  -H "Content-Type: application/json" \
  -H "Origin: http://localhost:3000" \
  -d '{"name":"Test Company","description":"Test","owner_id":"test-id"}' \
  -v

# Должен вернуть 200 OK или 201 Created
```

### Тест через браузер:

Откройте консоль браузера (F12) и выполните:

```javascript
fetch('http://localhost:8000/api/companies')
  .then(r => r.json())
  .then(data => console.log('✅ CORS работает!', data))
  .catch(err => console.error('❌ CORS ошибка:', err))
```

## 🐛 Troubleshooting

### Проблема: "Network Error" или "Failed to fetch"

**Решение:**
1. Проверьте что backend запущен
2. Проверьте порт (должен быть 8000)
3. Попробуйте открыть http://localhost:8000/docs

### Проблема: CORS ошибка после перезапуска

**Решение:**
1. Перезагрузите страницу frontend (Ctrl+R)
2. Очистите кэш браузера (Ctrl+Shift+R)
3. Перезапустите оба сервера

### Проблема: Работает в одном браузере, не работает в другом

**Решение:**
- Очистите кэш проблемного браузера
- Проверьте расширения браузера (могут блокировать запросы)
- Откройте в режиме инкогнито

## 📚 Дополнительная информация

### Документация FastAPI CORS:
https://fastapi.tiangolo.com/tutorial/cors/

### MDN CORS:
https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS

---

## ✅ Итоговый чеклист

После применения исправлений проверьте:

- [ ] Backend перезапущен
- [ ] Frontend перезапущен (или обновлена страница)
- [ ] Можно создать компанию
- [ ] Можно создать пользователя
- [ ] Можно создать проект
- [ ] Можно создать задачу
- [ ] Drag & drop работает
- [ ] Нет ошибок в консоли браузера

---

**Если всё работает - CORS настроен правильно! ✅**

