# 💾 Система сохранения данных

## Как это работает

### Автоматическое сохранение

Все данные **автоматически сохраняются** в файл `backend/data/database.json` после каждой операции:

- ✅ Создание (CREATE)
- ✅ Обновление (UPDATE)  
- ✅ Удаление (DELETE)

### Автоматическая загрузка

При **запуске сервера**:
1. Система пытается загрузить данные из `database.json`
2. Если файл найден - загружает существующие данные
3. Если файла нет - создаёт демо данные

### Структура файла

```json
{
  "users": {...},
  "companies": {...},
  "projects": {...},
  "roles": {...},
  "project_members": {...},
  "boards": {...},
  "tasks": {...}
}
```

## 📂 Расположение данных

```
backend/
├── data/
│   └── database.json    # Все данные системы
├── main.py
└── requirements.txt
```

## 🔄 Что сохраняется

### Автоматически сохраняются:

**Пользователи:**
- Создание нового пользователя
- Редактирование профиля
- Удаление пользователя

**Компании:**
- Создание компании
- Изменение информации
- Удаление компании

**Проекты:**
- Создание проекта
- Обновление данных
- Изменение статуса
- Удаление проекта

**Роли:**
- Создание роли
- Удаление роли

**Доски:**
- Создание доски
- Удаление доски

**Задачи:**
- Создание задачи
- Редактирование задачи
- Перемещение (drag & drop)
- Изменение статуса
- Удаление задачи

## 💡 Использование

### Обычная работа

Просто работайте с системой как обычно:

```bash
# Запустите сервер
cd backend
python main.py

# Все изменения сохраняются автоматически! ✨
```

### Перезапуск сервера

```bash
# Остановите сервер (Ctrl+C)
# Снова запустите
python main.py

# Все ваши данные загрузятся автоматически! 🎉
```

## 🔧 Управление данными

### Сброс к демо данным

**Способ 1: Удалить файл данных**
```bash
# Windows
del backend\data\database.json

# Linux/Mac
rm backend/data/database.json

# Перезапустите сервер - создадутся демо данные
```

**Способ 2: Переименовать файл**
```bash
# Сохраните текущие данные
mv backend/data/database.json backend/data/database.backup.json

# Перезапустите сервер
```

### Резервное копирование

**Ручное копирование:**
```bash
# Создайте копию
cp backend/data/database.json backend/data/backup_$(date +%Y%m%d).json
```

**Автоматическое (можно добавить в cron/планировщик):**
```bash
# Ежедневное резервное копирование
0 0 * * * cp /path/to/backend/data/database.json /path/to/backups/db_$(date +\%Y\%m\%d).json
```

### Восстановление данных

```bash
# Замените текущий файл бэкапом
cp backend/data/backup_20241126.json backend/data/database.json

# Перезапустите сервер
```

## 📊 Мониторинг

### Проверка данных

```bash
# Посмотрите содержимое файла
cat backend/data/database.json

# Или откройте в редакторе
code backend/data/database.json
```

### Размер данных

```bash
# Linux/Mac
du -h backend/data/database.json

# Windows
dir backend\data\database.json
```

## ⚠️ Важные замечания

### ✅ Что работает отлично:

- ✅ Автоматическое сохранение после каждого действия
- ✅ Автоматическая загрузка при запуске
- ✅ Поддержка всех операций CRUD
- ✅ UTF-8 поддержка (кириллица)
- ✅ Читаемый JSON формат
- ✅ Обработка ошибок

### ⚡ Ограничения текущей реализации:

- ⚠️ Файловое хранилище (не масштабируется для больших объёмов)
- ⚠️ Нет транзакций (в production нужна БД)
- ⚠️ Нет блокировок (один процесс за раз)
- ⚠️ Нет истории изменений

### 🚀 Для продакшена рекомендуется:

- 🔄 Перейти на PostgreSQL / MySQL
- 🔄 Добавить миграции (Alembic)
- 🔄 Настроить репликацию
- 🔄 Автоматические бэкапы
- 🔄 Мониторинг и логирование

## 🔐 Безопасность

### Рекомендации:

1. **Добавьте в .gitignore:**
   ```
   backend/data/
   *.json
   ```

2. **Регулярные бэкапы:**
   - Настройте автоматическое резервное копирование
   - Храните бэкапы в отдельном месте

3. **Права доступа:**
   ```bash
   # Ограничьте доступ к файлу данных
   chmod 600 backend/data/database.json
   ```

## 📈 Миграция на БД

Когда нужна реальная база данных:

### PostgreSQL

```python
# 1. Установите драйвер
pip install psycopg2-binary sqlalchemy

# 2. Настройте подключение
from sqlalchemy import create_engine
engine = create_engine('postgresql://user:pass@localhost/dbname')

# 3. Создайте модели
from sqlalchemy.ext.declarative import declarative_base
Base = declarative_base()

class User(Base):
    __tablename__ = 'users'
    # ... определение полей
```

### MongoDB

```python
# 1. Установите драйвер
pip install pymongo motor

# 2. Настройте подключение
from motor.motor_asyncio import AsyncIOMotorClient
client = AsyncIOMotorClient('mongodb://localhost:27017')
db = client.tasktracker
```

### SQLite (быстрая альтернатива)

```python
# Встроен в Python, просто измените путь:
import sqlite3
conn = sqlite3.connect('backend/data/database.db')
```

## 📝 Логи сохранения

При каждом сохранении в консоли сервера:

```
✅ Данные сохранены: backend/data/database.json
```

При загрузке:

```
✅ Данные загружены из backend/data/database.json
   👥 Пользователей: 3
   🏢 Компаний: 1
   📁 Проектов: 1
   🛡️ Ролей: 4
   📋 Досок: 1
   ✅ Задач: 4
```

При создании демо данных:

```
ℹ️ Файл данных не найден, будут созданы демо данные
🔧 Создаём демо данные...
✅ Демо данные созданы и сохранены
```

## 🎯 FAQ

**Q: Данные сохраняются сразу или с задержкой?**  
A: Сразу после каждой операции изменения.

**Q: Можно ли редактировать database.json вручную?**  
A: Да, но остановите сервер перед редактированием.

**Q: Что если файл повреждён?**  
A: Удалите его, сервер создаст новый с демо данными.

**Q: Можно ли использовать несколько серверов одновременно?**  
A: Нет, для этого нужна реальная БД с блокировками.

**Q: Где логи ошибок?**  
A: В консоли сервера, начинаются с ❌.

**Q: Как посмотреть что сохранено?**  
A: Откройте `backend/data/database.json` в любом текстовом редакторе.

---

**Ваши данные теперь сохраняются автоматически! 💾**

