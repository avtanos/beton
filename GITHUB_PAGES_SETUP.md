# 🚀 Быстрая настройка GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название: `beton` (или ваше имя)
3. Описание: "АСУ ТП Бетонного завода"
4. Выберите **Public** (или Private с GitHub Pro)
5. **НЕ** добавляйте README, .gitignore или license (они уже есть)

## Шаг 2: Инициализируйте Git и загрузите код

```bash
# Если Git еще не инициализирован
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: АСУ ТП Бетонного завода"

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/beton.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код
git push -u origin main
```

## Шаг 3: Настройте GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите **"GitHub Actions"**
4. Сохраните настройки

## Шаг 4: Обновите base path (если нужно)

### Если репозиторий называется `beton`:
✅ Ничего менять не нужно - уже настроено!

### Если репозиторий называется по-другому:

1. Обновите `.github/workflows/deploy.yml`:
   ```yaml
   env:
     VITE_BASE_PATH: /ваше-имя-репозитория/
   ```

2. Обновите `frontend/vite.config.production.ts`:
   ```typescript
   base: process.env.VITE_BASE_PATH || '/ваше-имя-репозитория/',
   ```

3. Обновите `frontend/src/main.tsx`:
   ```typescript
   const basePath = import.meta.env.VITE_BASE_PATH || '/ваше-имя-репозитория/'
   ```

### Если репозиторий называется `username.github.io`:

Используйте `/` вместо `/beton/` во всех местах выше.

## Шаг 5: Проверьте деплой

1. После push в `main`, GitHub Actions автоматически запустится
2. Проверьте статус в **Actions** вкладке вашего репозитория
3. После успешного деплоя (обычно 2-3 минуты) сайт будет доступен:
   - `https://YOUR_USERNAME.github.io/beton/`

## Шаг 6: Настройте API (опционально)

Если у вас есть backend API:

1. Перейдите в **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **New repository secret**
3. **Name**: `VITE_API_URL`
4. **Value**: `https://your-backend-api.com/api`
5. Обновите `.github/workflows/deploy.yml`:
   ```yaml
   - name: Build
     run: |
       cd frontend
       npm run build:prod
     env:
       VITE_BASE_PATH: /beton/
       VITE_API_URL: ${{ secrets.VITE_API_URL }}
   ```

## ✅ Готово!

Ваш проект теперь автоматически деплоится на GitHub Pages при каждом push в `main`!

### Полезные ссылки:
- [Полная документация по деплою](docs/DEPLOYMENT.md)
- [Настройка GitHub](docs/GITHUB_SETUP.md)
- [Бизнес-процессы](docs/BUSINESS_PROCESSES.md)

## 🔧 Устранение проблем

### Деплой не запускается
- Убедитесь, что GitHub Pages включен и выбран источник "GitHub Actions"
- Проверьте, что workflow файл находится в `.github/workflows/deploy.yml`
- Убедитесь, что вы push'ите в ветку `main` или `master`

### Сайт не открывается / 404 ошибка
- Проверьте, что base path правильный (должен совпадать с именем репозитория)
- Убедитесь, что в URL есть слеш в конце: `/beton/` а не `/beton`
- Подождите несколько минут после деплоя (кэш GitHub)

### API запросы не работают
- Убедитесь, что backend API доступен и CORS настроен правильно
- Проверьте переменную окружения `VITE_API_URL` в secrets
- Откройте консоль браузера для проверки ошибок

